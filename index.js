"use strict";
const sum = function (a, b) {
  return a + b;
};

sum(1, 2);
const minus = function (a, b) {
  return a - b;
};

minus(4, 2);
const mult = function (a, b) {
  return a * b;
};

mult(1, 2);
